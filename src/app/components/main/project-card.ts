export class ProjectCard {
        title: string;
        description: string;
        link: string;
        route:string;

        constructor(title: string,description: string,link: string,route:string){
                this.title=title,
                this.description=description
                this.link=link,
                this.route=route
        }
}

